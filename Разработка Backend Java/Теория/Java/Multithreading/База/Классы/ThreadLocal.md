Класс для хранения переменных 
Хранит свои значения переменных для каждого потока, который его использует

По сути Map<Thread, T> с потоком в *key* и значением переменной в *value*

Начиная с JDK 24 появились более быстрые [[ScopedValues]]